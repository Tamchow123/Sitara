# Environment / secrets
.env
.env.local
.env.*.local

# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/
.ruff_cache/
apps/api/staticfiles/
# Local filesystem design-image storage (private; never committed)
apps/api/private-design-images/

# Node / Next.js
node_modules/
apps/web/.next/
apps/web/out/
*.tsbuildinfo

# OS noise
Thumbs.db
.DS_Store

# Local-only docker compose port overrides (never committed)
compose.override.yaml
docker-compose.override.yml

# run-phase review-council transient state (config is committed; live state is not)
.claude/review/runtime/*
!.claude/review/runtime/.gitignore
.claude/review/reports/*
!.claude/review/reports/.gitignore
